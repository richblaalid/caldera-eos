# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# AI Tool Directories
# Only .claude/ is tracked (contains skills and commands for Claude Code)
# All other AI tool directories are ignored
.agents/
.cursor/

# ember/ level tool directories are ignored via ember/.gitignore
